---
---

return {
  'qvalentin/helm-ls.nvim',
  ft = 'helm',
  opts = {
    conceal_templates = {
      enable = true,
    },
    indent_hits = {
      enable = true,
      only_for_current_file = true,
    },
    action_highlight = {
      enable = true,
    },
    -- leave empty or see below
  },
}
